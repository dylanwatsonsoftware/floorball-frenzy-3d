#!/bin/sh
set -eu

godot_binary="${GODOT_BIN:-godot}"

mkdir -p build/web
"$godot_binary" --headless --path . --export-release Web build/web/index.html
./scripts/harden-service-worker build/web/index.service.worker.js
